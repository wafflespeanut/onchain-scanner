name: deploy-lambda
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install deps
      run: |
        sudo apt update
        sudo apt install -y libssl-dev
    - name: Install cargo lambda
      run: |
        python -m pip install --upgrade pip
        pip install cargo-lambda
    - name: Build lambda function
      run: |
        cd FetchOnchainBars
        cargo lambda build --release --arm64
    - name: Deploy lambda function
      run: |
        cd FetchOnchainBars
        cargo lambda deploy --region ${{ secrets.AWS_REGION }} --role ${{ secrets.AWS_IAM_ROLE }} -- --access-key ${{ secrets.AWS_ACCESS_KEY_ID }} --secret-key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
